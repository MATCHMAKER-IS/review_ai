# ④レビュー学習AI 引き継ぎメモ

CLAUDE.md v0.1 の補足。担当：福井。最終更新 2026-07-24。

このファイルは**「なぜそうしたか」を残すため**のものです。理由を知らないと
善意で元に戻されて壊れる箇所が複数あります。

---

## 1. 最終構成

```
Zoho Desk（下書き作成時）      ─┐
   POST /api/messages kind=ai  │
                                ├─→ Amplify (Next.js / Lambda)
Zoho Desk（返信送信時）        ─┘      │  ペアリング → diff → 切り分け
   POST /api/messages kind=sent        │  ※ここでLLMは呼ばない
                                       ▼
                                  PostgreSQL
                                       │
                                       │ レビューが5件溜まったら
                                       ▼
                              ④レビュー学習AI（OpenAI）
                                       │
                                       ▼
                              メモリ修正案 → 承認画面 → メモリ更新
                                                              │
                              ②回答生成AI ←── GET /api/memory ┘
```

**Deluge は「POSTするだけ」**。判定・保存・学習はすべてAmplify側です。
当初はDeluge内でOpenAIを呼ぶ構成でしたが、40秒のタイムアウト制限と
エスケープ処理の問題（§4参照）で断念しました。

---

## 2. API

| メソッド | パス | 用途 |
|---|---|---|
| POST | `/api/messages` | **Desk からの受け口。**①②を別々に受け取りペアリング |
| POST | `/api/reviews` | ①②をまとめて受け取る版（旧仕様。互換のため残置） |
| GET | `/api/memory?staff_id=` | ②生成AIに渡すメモリ。`rendered` をプロンプトに差し込む |
| POST | `/api/learning/analyze` | ④の分析起動。EventBridge から叩く想定 |
| GET | `/api/export/reviews` | 三井さんへの受け渡し |
| GET | `/api/health` | DB接続確認。**デプロイ後まずここを叩く** |

### POST /api/messages

```json
{
  "ticket_id": "1234567",   // 必須
  "message":   "本文",       // 必須
  "kind":      "ai",         // 必須  "ai" | "sent"
  "staff_id":  "890123",     // 任意（強く推奨）
  "prompt_version": "3"      // 任意（kind=ai のとき推奨）
}
```

`kind="sent"` が届いた時点で、同じチケットの未ペアの下書きと突き合わせて
レビューを1件作ります。

---

## 3. 設計判断とその理由

**この節の内容を変更する場合は、理由を確認してからにしてください。**

### 3-1. 帰属の判定にLLMを使っていない

「判断AIのミスか、生成AIのミスか」は `lib/learning/classify.ts` が
ルールベースで決めています。§7の原則をそのままコードにしただけです。

ここにLLMを挟むと、④の誤判定が①②の誤りと混ざります。**精度が上がらない
ときに、判断AIが悪いのか生成AIが悪いのか④が悪いのかを切り分けられなく
なります。** そうなったシステムは改善しようがありません。

LLMの仕事は「同じ種類の誤りの束から共通パターンを言語化する」だけです。

### 3-2. 修正の「大きさ」で学習の要否を決めていない

当初は `diff_ratio` が一定以上のものだけを学習対象にしていましたが、
これは**実データで誤りと判明しました。**

```
AI  : 19:30の誤りとのこと、承知いたしました。
送信: 19:30の誤りとのこと、承知しました(^^♪
```

本文100文字に対して編集距離6、`diff_ratio` は **0.059**。当初の閾値0.1を
下回るため捨てられていました。しかしこれはそのコーディネーターの癖そのもので、
学習価値が最も高い差分です。**修正の大きさと学ぶ価値は無関係です。**

現在は「正規化後に1文字でも変わったか」で判定します。`diff_ratio` は
§7の指標としてのみ残しています。

### 3-3. 1件のレビューからルールを作らない

- `LEARN_MIN_REVIEWS=5` — これ未満では分析を実行できない
- `LEARN_MIN_OCCURRENCES=2` — 2件以上に現れたパターンしかルール化しない

単発の修正は「その日の気分」か「その案件固有の事情」であることが多く、
ルール化するとメモリが濁ります。加えて1件1ルールだと§3-5の30件上限を
数日で使い切ります。

シードデータの7件目は**意図的に1件しかないパターン**です。これがルール化
されなければ閾値が効いています。

### 3-4. 文体ルールには適用条件を必ず書かせる

「顔文字を時々つける」のようなルールを許すと、生成AIは謝罪メールにも
顔文字を入れます。日程トラブル・お断り・退会・返金・苦情への返信で
くだけた表現を使うと重大な事故になります。

プロンプトで次を強制しています。

- 文体ルールには「どの場面か」を必ず含める
- くだけた文体のルールを作ったら、禁止場面を `ng_findings` にセットで挙げる

**承認画面で、適用条件が書かれていない提案は見送ってください。** 事故の種です。

### 3-5. `action` を人に選ばせていない

`inferAction()` が本文の変化を見て approve / edit を自動判定します。
ボタンを押し分けさせると「承認」ばかり押されて、§7の無修正承認率が
実態より高く出ます。

### 3-6. プロンプト版をまたいだバッチを検知する

プロンプトが変わればAIの文体も変わります。版をまたいだレビューをまとめて
学習させると、**プロンプト変更の影響をコーディネーターの癖として覚えます。**

④が検知して提案のnoteに警告を付けます。ブロックはしません（版が頻繁に
変わると永久に5件溜まらなくなるため）。

### 3-7. §3-5 の契約を変えずに引退判定を実装した

30件上限に達したとき、どのルールを消すかの判断材料が要ります。ルールに
使用回数を持たせるのが素直ですが、それは§3-5の契約変更になります。

`rule_usage` テーブルを別に作り、Draftの `used_memory_rules` から加算する
形にしました。契約は無傷のまま、「一度も適用されたことがないルール」から
引退候補を出せます。

### 3-8. 切り分け結果を書き込み時に確定させる

`reviews.fault` 列に保存します。判定はルールベースで決定的なので、
同じ入力から二度計算する理由がありません。**SQLで直接「判断ミス率」
「生成ミス率」が出せます。**

### 3-9. AIはメモリを書き換えない

`lib/learning/apply.ts` はUIの操作からしか呼ばれません。
§3-5「AIが提案 → 本人が承認」および§5「自動適用はPhase2」のとおりです。

---

## 4. Deluge の制約（実測で確定）

Delugeから直接OpenAIを叩くのをやめた理由です。**他のメンバーが同じ罠を
踏まないよう共有してください。**

| 記述 | 結果 |
|---|---|
| `"\n"` | 改行1文字。正常 |
| `"\""` | ダブルクォート1文字。正常 |
| `"\\n"` | **バックスラッシュ + 実改行。JSONを壊す** |
| `"\r"` `"\t"` | エスケープとして解釈されない（2文字のまま）|

**Deluge でバックスラッシュ1文字を作る手段がありません。** JSONの
エスケープシーケンスは書けない前提で設計する必要があります。

その他の制約：

- `replaceAll` の置換文字列に含めたバックスラッシュは失われる
- 複数行の文字列リテラルは非対応
- `for each x in expr.method()` は非対応。変数に代入してから渡す
- `invokeurl` は40秒でタイムアウト。変更不可
- `Map.toString()` は値の中のダブルクォート・改行の扱いが不明

現在の構成では自前サーバーが受けるので、これらはもう踏みません。
`payload.toString()` をそのまま使えます。

---

## 5. 未解決の課題

| 課題 | 状態 | 誰が |
|---|---|---|
| **OpenAI の課金** | 「You exceeded your current quota」。**④の分析はこれが直るまで動かない** | 要確認 |
| **`decision_ok` の契約追加** | §3-4への3項目追加が承認待ち | 大山 |
| **Managed Prompt の廃止** | `v1/prompts` が2026-11-30に停止。プロンプトをコードへ移す必要あり | 三井 |
| **§6-3 個人情報の解釈** | `ai_body`/`sent_body` に会員名が入る。DB暗号化と文言確定 | 大山 |
| **画面の認証** | 現状なし。Cognito か Basic認証を被せる | 福井 |
| **分析のタイムアウト** | Amplifyの上限次第。超えたら EventBridge + 別Lambda へ | 福井 |

### `decision_ok` を推す理由

判断AIの正否は**2つの文面だけからは決められません。** 内容ごと書き換わって
いた場合、それが「宛先が違ったから全部書き直した」のか「言い回しが
気に入らなかった」のかは文面から読めません。

現在はそういうケースを `unknown` として学習対象から外しています。つまり
**`decision_ok` が無いと、一番学習価値の高いケースが丸ごと捨てられます。**

承認画面にチェックボックス1個を足すだけで、切り分けが推論ではなく事実に
なります。

---

## 6. 運用上の注意

### コネクションプール

Amplify の SSR は Lambda で動きます。同時実行数だけインスタンスが増え、
それぞれがプールを持ちます。`max: 10` にすると同時実行30で300接続を
要求してRDSが落ちます。

`lib/pg.ts` は `max: 2` です。**増やさないでください。** 同時実行が増える
見込みなら RDS Proxy を挟んでください。

### DB接続の遅延初期化

`next build` はルートのモジュールを読み込みます。モジュール読み込み時に
`DATABASE_URL` を要求するとビルドが落ちるため、接続は最初のクエリまで
遅延させています。

副作用として、**`DATABASE_URL` の設定漏れはビルドでは検知できず
`/api/health` で初めて落ちます。**

### VPC 内の RDS に繋ぐ場合

Lambda を VPC に入れると OpenAI API に出られません。NAT Gateway か
VPC Endpoint が必要です。外向き通信をするのは④の分析だけなので、
そこを別Lambdaに切り出すと構成が単純になります。

### Git

- `node_modules/` はコミットしない（`.gitignore` 済み）
- **`package-lock.json` はコミットする。** 無いと `npm ci` が失敗し、
  各人のビルドで依存バージョンがずれます

---

## 7. 次にやること

1. **OpenAI の課金を解消**（これが最優先。他が全部止まる）
2. `package-lock.json` をコミットして Amplify のビルドを通す
3. `/api/health` で DB 接続を確認
4. `db/schema.sql` と `db/002_messages.sql` を流す
5. `/api/messages` に ai → sent の順でPOSTし `paired: true` を確認
6. Desk のワークフローを2本作る（下書き作成時 / 返信送信時）
7. 実データが5件溜まったら「振り返りを実行」

### 検証状況

`classify.ts` の分岐7パターンと `diff.ts` の計算は実行して確認済みです。
**それ以外（Next.jsのビルド、PostgreSQL、APIの動作）は未検証です。**

特に確認してほしいのは、往復の多い案件で下書きが複数溜まったときの
**ペアリングの挙動**です。この実装の一番の弱点です。
